HIGH

- `_bmad-output/implementation-artifacts/14-4-live-integration-test-runs-c70-close.md:19,203` and `_bmad-output/cross-llm-reviews/story-14-4-review-prompt.md:44,59,485` cite Epic 12 retro Action #4 as `L162`, but the live source is `_bmad-output/implementation-artifacts/epic-12-retro-2026-06-01.md:163`. That directly breaks Story 14.4’s own “all citations verified pre-write” claim. Concrete fix: change every `Epic 12 retro L162 Action #4` reference in the story spec and review prompt to `L163`.

- `.github/workflows/dogfood-integration.yml:231-277` runs on `ubuntu-latest`, installs only Python deps, and then probes/runs CLI-backed coverage that depends on `codex`, `copilot`, and `claude` being on `$PATH`. The repo itself documents that Codex/Copilot must already be installed externally (`pyproject.toml:79-90`), and the live tests skip when those binaries are absent (`tests/integration/test_codex_cli_live.py:40-42`, `tests/integration/test_copilot_cli_live.py:25-27`). As written, the job has no repo-owned way to satisfy the CLI half of Story 14.4 on a stock runner, so `_TESTED_UP_TO` verification for those adapters is best-effort rather than a delivered mechanism. Concrete fix: either install the required CLIs in the workflow, or move the job to a runner label/image that guarantees them and document that prerequisite explicitly.

MED

- `.github/workflows/dogfood-integration.yml:268-273` does not implement the advertised `NOT FOUND` fallback correctly. `observed="$($bin --version 2>&1 | head -1 || echo 'NOT FOUND')"` runs in a pipeline without `pipefail`, so a missing binary yields the shell error text instead of `NOT FOUND` (reproduced locally with a fake binary name). Concrete fix: guard with `command -v "$bin" >/dev/null 2>&1` before invocation, or branch on the command result instead of relying on `||` after `head`.

- `.github/workflows/dogfood-integration.yml:215-219` tells the operator the wrong secrets-to-tests mapping. It says `OPENAI_API_KEY` is used by `judge`/`judge_calibrate` and `ANTHROPIC_API_KEY` only by `claude_agent_sdk`, but the judge live tests actually gate on `ANTHROPIC_API_KEY` (`tests/integration/test_judge_live.py:34-35`, `tests/integration/test_judge_calibrate_live.py:42-43`), while `OPENAI_API_KEY` is for `openai_agents_sdk` and Codex (`tests/integration/test_openai_agents_sdk_live.py:42-44`, plus the codex note). Concrete fix: rewrite the prerequisite comment so `ANTHROPIC_API_KEY` is called out for `claude_agent_sdk`, `judge`, and `judge_calibrate`, and `OPENAI_API_KEY` is called out for `openai_agents_sdk` and Codex.

- `.github/workflows/dogfood-integration.yml:321-327` reports an all-skipped / zero-pass run as “all live test files exited cleanly” with no explicit notice that no live evidence was actually produced. That weakens the story’s mechanism-vs-evidence framing in the exact operator-misconfiguration path the workflow is supposed to make honest. Concrete fix: track aggregate `passed`/`skipped` totals and emit a separate summary line plus `::notice::` when `passed == 0` (or all files are skipped / exit 5) telling the operator to configure the missing prerequisites before treating the run as evidence.

- `_bmad-output/implementation-artifacts/sprint-status.yaml:38` still says `last_updated: 2026-06-03`, but the story claims Story 14.4 advanced that field to `2026-06-04` (`_bmad-output/implementation-artifacts/14-4-live-integration-test-runs-c70-close.md:139,226`). That leaves the project status ledger internally inconsistent on AC-14.4.6. Concrete fix: bump `last_updated` in `sprint-status.yaml` to `2026-06-04`, or remove the claim from the story if that field is intentionally left untouched.

- `_bmad-output/implementation-artifacts/14-4-live-integration-test-runs-c70-close.md:155,190,246,258` and `_bmad-output/implementation-artifacts/sprint-status.yaml:165` hardcode the workflow change as `+60 lines`, but the actual workflow diff is `@@ -208,3 +208,122` and `git diff --stat` reports `119 insertions` in `.github/workflows/dogfood-integration.yml`. That means the story’s own “workflow file size accounting” check cannot pass against the current tree. Concrete fix: update those notes to match the real delta at HEAD, or drop exact line-count claims and describe the shipped job structurally instead.

LOW

- None.
