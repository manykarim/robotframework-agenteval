---
name: example-search
description: Example skill for the agenteval starter project. Activates on prompts asking for search-related tasks.
allowed-tools: ["search", "fetch"]
disable-model-invocation: false
---

# Example Search Skill

This is a sample skill scaffolded by `agenteval init`. It shows the four
required frontmatter fields — `name`, `description`, `allowed-tools`, and
`disable-model-invocation` — that agenteval inspects with `Skill.Get Frontmatter`
and `Skill.Should Be Valid Frontmatter`.

Replace this content with your own skill instructions when you're ready.
